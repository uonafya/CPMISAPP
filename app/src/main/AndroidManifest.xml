<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abedkiloo.cpmisapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".OVC_CARE_Forms.Activities.Form1A"
            android:label="@string/form_1a_detail_activity" />
        <activity android:name=".Forms" />
        <activity android:name=".OVC_CARE" />
        <activity
            android:name=".OVC_CARE_Forms.Activities.CSI"
            android:label="@string/title_activity_csi"></activity>
    </application>

</manifest>